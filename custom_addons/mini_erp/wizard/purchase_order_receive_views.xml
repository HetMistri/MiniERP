<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="view_purchase_order_receive_form" model="ir.ui.view">
            <field name="name">purchase.order.receive.form</field>
            <field name="model">purchase.order.receive</field>
            <field name="arch" type="xml">
                <form string="Receive Purchase Order">
                    <sheet>
                        <group>
                            <field name="order_id" readonly="1"/>
                        </group>
                        <notebook>
                            <page string="Items to Receive">
                                <field name="line_ids">
                                    <list editable="bottom" create="0" delete="0">

                                        <field name="purchase_line_id" invisible="1"/>
                                        <field name="product_id" invisible="1"/>

                                        <field name="qty_ordered" readonly="1"/>
                                        <field name="qty_received"/>

                                    </list>
                                </field>
                            </page>
                        </notebook>
                    </sheet>
                    <footer>
                        <button name="action_validate" string="Validate" type="object" class="btn-primary"/>
                        <button string="Cancel" class="btn-secondary" special="cancel"/>
                    </footer>
                </form>
            </field>
        </record>

        <record id="action_purchase_order_receive" model="ir.actions.act_window">
            <field name="name">Receive Purchase Order</field>
            <field name="res_model">purchase.order.receive</field>
            <field name="view_mode">form</field>
            <field name="target">new</field>
        </record>
    </data>
</odoo>
